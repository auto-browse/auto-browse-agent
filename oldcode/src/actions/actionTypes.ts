import { ActionType } from "../types";

export const TEST_CONNECTION = ActionType.TEST_CONNECTION;
export const HIGHLIGHT_LINKS = ActionType.HIGHLIGHT_LINKS;

// Re-export the enum
export { ActionType };
